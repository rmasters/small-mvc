This is a small-footprint MVC framework, written in PHP. It requires at least
PHP 5.3 as it uses namespaces and closures.

I wouldn't reccommend using it, it's messy, untidy and has a bad smell. Don't
even look at Db.
